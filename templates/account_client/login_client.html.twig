{% extends 'base_co.html.twig' %}

{% block title %}Connexion au site{% endblock %}

{% block body %}

    {% block form %}
            <div id="form-co" class="col-6">
                <div class="col-6 mx-auto shadow p-3 border">
                    <h1>CONNEXION</h1>
                    <hr>
                    {% if hasError%}
                        <div class="alert alert-danger text-center"><p>L'email ou le mot de passe n'est pas valide</p></div>
                    {% endif %}
                    {% for label, messages in app.flashes %}
                        <div class="container text-center">
                            <div class="alert alert-{{ label }}">
                                {% for message in messages %}
                                    <p><strong>{{ message }}</strong></p>
                                {% endfor %}
                            </div>
                        </div>
                    {% endfor %}
                    <p class="text-center mt-4 mb-4">Bienvenue ! Vous n'êtes pas encore inscrit?<a href="{{ path('account_register_client') }}"> Cliquez ici.</a></p>
                    <form action="{{ path('account_login_client') }}" method="post">
                        <div class="form-group">
                            <label for="email">Email</label>
                            <input type="text" name="_username" id="email" class="form-control"
                                placeholder="Entrez votre email" required value="{{ username }}">
                        </div>
                        <div class="form-group">
                            <label for="password">Mot de passe</label>
                            <input type="password" name="_password" id="password" class="form-control"
                                placeholder="Entrez votre mot de passe" required>
                        </div>
                        <div class="form-group text-center">
                            <button type="submit" class="btn btn-dark text-light mt-3">Connexion</button>
                        </div>
                    </form>
                </div>
    {% endblock %}

{% endblock %}
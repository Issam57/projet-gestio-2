<header>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="collapse navbar-collapse" id="navbarColor02">
            <img class="navbar" src="/image/Logo Gesti'O (blanc).png" alt="logo" />
            <ul class = "navbar-nav">
                <li class = "nav-item ml-5">
                    <a class="nav-link" href="{{ path('accueil_client') }}">Accueil</a>
                </li>
                <li class = "nav-item ml-5">
                    <a class="nav-link" href="{{ path('news') }}">News</a>
                </li>
                <li class = "nav-item ml-5">
                    <a class="nav-link" href="{{ path('restos_index') }}">Restaurant</a>
                </li>
            </ul>
            <ul class="navbar-nav ml-auto">
                {% if app.user %}
                    <li class="nav-item dropdown">
                        <a href="#" class="nav-link dropdown-toggle" data-toggle="dropdown" id="accountDropdownLink">
                            <img src="/image/avatar.jpg" class="avatar avatar-mini" alt="avatar">{{ app.user.fullName }}
                        </a>
                        <div class="dropdown-menu dropdwon-menu-right" aria-labelledby="accountDropdownLink">
                            <a href="{{ path('account_index') }}" class="dropdown-item">Mon compte</a>
                            <a href="{{ path('account_client_profile') }}" class="dropdown-item">Modifier mon profil</a>
                            <a href="{{ path('account_client_password') }}" class="dropdown-item">Modifier mot de passe</a>
                            <div class="dropdown-divider"></div>
                            <a href="{{ path('account_logout_client') }}" class="dropdown-item">Déconnnexion</a>
                        </div>

                    </li>
                {% else %}
                    <li class="nav-item border">
                        <a href="{{ path('account_register_client') }}" class="nav-link">Inscription</a>
                    </li>
                    <li class="nav-item border ml-2">
                        <a href="{{ path('account_login_client') }}" class="nav-link">Connexion</a>
                    </li>
                {% endif %}
            </ul>
        </div>
    </nav>
    {% for label, messages in app.flashes %}
        <div class="container text-center mt-3">
            <div class="alert alert-{{ label }} border">
                {% for message in messages %}
                    <p><strong>{{ message }}</strong></p>
                {% endfor %}
            </div>
        </div>
    {% endfor %}
</header>